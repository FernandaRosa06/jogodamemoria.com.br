document.addEventListener('DOMContentLoaded', () => {
    const retryButton = document.getElementById('retry-button');
    retryButton.addEventListener('click', () => {
        window.location.href = 'index.html'; // Altere para a URL correta do seu jogo
    });
});
